import InfoPage from "@/components/InfoPage";

export default function NotFoundPage() {
  return (
    <InfoPage
      header="404 - Página No Encontrada"
      description="La página que está buscando podría haber sido eliminada o no existir."
    />
  );
}
